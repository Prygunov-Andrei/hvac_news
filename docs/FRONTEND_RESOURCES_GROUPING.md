# Задание: Группировка ресурсов по разделам

## Изменения в API

### Новая структура данных

В ответе API `/api/references/resources/` теперь добавлено поле `section`:

```json
{
  "id": 329,
  "name": "AC Service Tech (YouTube)",
  "url": "https://www.youtube.com",
  "logo": null,
  "description": "Крупнейший учебный HVAC-канал...",
  "section": "Specialized Niche Resources (Специализированные Нишевые Ресурсы)"
}
```

**Важно:** Поле `description` больше не содержит название раздела в квадратных скобках. Название раздела теперь в отдельном поле `section`.

## Задание

Реализовать группировку ресурсов на странице отображения списка ресурсов:

### Вариант 1: Группировка по разделам (рекомендуется)
- Сгруппировать все ресурсы по полю `section`
- Отобразить разделы как заголовки групп
- Под каждым разделом показать список ресурсов этого раздела
- Ресурсы внутри каждой группы отсортировать по алфавиту (по полю `name`)

### Вариант 2: Алфавитная группировка
- Если поле `section` пустое или не используется, группировать по первой букве названия
- Отобразить буквы как заголовки групп
- Под каждой буквой показать ресурсы, начинающиеся с этой буквы

### Пример структуры отображения (Вариант 1):

```
📁 North America and Latin America
  • ACCA Blog
  • ACHR News
  • ASHRAE

📁 Europe & Global Specialized
  • ACR News
  • Cooling Post
  • Eurovent

📁 Asia & Pacific
  • JARN
  • Cooling India
```

## Технические детали

- **Эндпоинт:** `GET /api/references/resources/`
- **Права доступа:** Публичный доступ (не требует аутентификации)
- **Всего ресурсов:** ~137

## Примечания

- Если у ресурса поле `section` пустое (`null` или пустая строка), можно отображать его в группе "Прочие" или "Без раздела"
- Ресурсы внутри групп должны быть отсортированы по алфавиту
- Можно добавить возможность переключения между группировкой по разделам и алфавитной группировкой
