# Задание: Карточка источника новостей

## Описание
Необходимо реализовать детальную карточку источника новостей, которая открывается по клику на строку в таблице источников. Карточка должна отображать всю информацию об источнике и позволять редактировать ключевые параметры.

## API Endpoints

**Базовый URL:** `/api/references/resources/`

### Получение источника
```
GET /api/references/resources/{id}/
```

**Аутентификация:** Не требуется (для чтения)

**Ответ:**
```json
{
  "id": 1,
  "name": "ACHR News",
  "url": "https://www.achrnews.com",
  "section": "North America and Latin America",
  "description": "Описание источника",
  "logo": "/media/resources/logos/achr.jpg",
  "source_type": "auto",
  "language": "en",
  "custom_search_instructions": "Кастомные инструкции для LLM...",
  "internal_notes": "Служебные заметки...",
  "statistics": {
    "total_news_found": 150,
    "total_searches": 10,
    "success_rate": 90.0,
    "error_rate": 10.0,
    "is_active": true,
    "ranking_score": 75.5
  },
  "is_problematic": false,
  "is_auto_searchable": true,
  "requires_manual_input": false
}
```

### Обновление источника
```
PATCH /api/references/resources/{id}/
```

**Аутентификация:** Требуется (JWT токен в заголовке `Authorization: Bearer {token}`)

**Тело запроса:**
```json
{
  "language": "es",
  "custom_search_instructions": "Новые инструкции...",
  "internal_notes": "Обновленные заметки...",
  "source_type": "hybrid"
}
```

**Ответ при успехе:** `200 OK` с обновленным объектом источника

**Ответ при ошибке валидации:** `400 Bad Request` с деталями ошибок:
```json
{
  "name": ["Источник с таким названием уже существует."],
  "url": ["URL должен начинаться с http:// или https://"]
}
```

## Структура карточки

### 1. Заголовок карточки
- **Название источника** (крупный шрифт, жирный)
- **URL** (ссылка, открывается в новой вкладке)
- **Секция/Регион** (бейдж)
- **Статус активности** (зеленый/серый индикатор)

### 2. Основная информация (вкладка "Основное")
- **Название** (редактируемое поле)
- **URL** (редактируемое поле)
- **Секция** (выпадающий список или текстовое поле)
- **Описание** (многострочное текстовое поле)
- **Логотип** (отображение + возможность загрузки)

### 3. Настройки поиска (вкладка "Настройки поиска")
- **Тип источника** (радио-кнопки или выпадающий список):
  - 🤖 Автоматический поиск
  - ✋ Ручной ввод
  - ⚙️ Гибридный (с кастомными инструкциями)
  
- **Язык источника** (выпадающий список с флагами):
  - 🇷🇺 Russian (Русский)
  - 🇺🇸 English (Английский)
  - 🇪🇸 Spanish (Испанский)
  - 🇩🇪 German (Немецкий)
  - 🇵🇹 Portuguese (Португальский)
  - 🇫🇷 French (Французский)
  - 🇮🇹 Italian (Итальянский)
  - 🇹🇷 Turkish (Турецкий)
  - 🇸🇦 Arabic (Арабский)
  - 🇨🇳 Chinese (Китайский)
  - 🇯🇵 Japanese (Японский)
  - 🇰🇷 Korean (Корейский)
  - и другие...

- **Кастомные инструкции для LLM** (многострочное текстовое поле):
  - Показывается только для типов "hybrid" и "manual"
  - Подсказка: "Специальные инструкции для LLM по поиску новостей на этом источнике"
  - Плейсхолдер: "Оставьте пустым для использования стандартного промпта"

### 4. Служебная информация (вкладка "Служебное")
- **Служебные заметки** (многострочное текстовое поле):
  - Подсказка: "Служебные заметки о источнике. Видны только администраторам."
  - Используйте для хранения служебной информации, замечаний, проблем и т.д.

### 5. Статистика (вкладка "Статистика")
- **Общая статистика:**
  - 📰 Всего найдено новостей: 150
  - 🔍 Всего выполнено поисков: 10
  - ✅ Успешность: 90.0%
  - ⚠️ Процент ошибок: 10.0%
  - 📊 Рейтинговый балл: 75.5
  - 📅 Последний поиск: 07.01.2026 15:30
  - 📅 Последняя новость: 05.01.2026 12:00

- **Периодическая статистика:**
  - 📰 Новостей за 30 дней: 25
  - 📰 Новостей за 90 дней: 75
  - 🔍 Поисков за 30 дней: 3

- **Статус:**
  - Активность: ✅ Активен / ❌ Неактивен
  - Проблемность: ⚠️ Проблемный (error_rate >= 30%) / ✅ Нормальный

### 6. Действия (кнопки в футере карточки)
- **Сохранить** (основная кнопка, сохраняет все изменения)
- **Отмена** (закрывает карточку без сохранения)
- **Удалить** (красная кнопка, с подтверждением)

## UI/UX Требования

### Открытие карточки
- Клик на любую строку в таблице источников открывает карточку
- Карточка открывается в модальном окне или side panel
- Можно закрыть по клику вне карточки или на кнопку "X"

### Вкладки
- Использовать табы для организации контента:
  - "Основное"
  - "Настройки поиска"
  - "Служебное"
  - "Статистика"

### Визуальные индикаторы
- **Тип источника:**
  - 🤖 Автоматический - зеленый цвет
  - ✋ Ручной ввод - красный цвет
  - ⚙️ Гибридный - желтый цвет

- **Язык:**
  - Отображать флаг страны рядом с названием языка
  - Выпадающий список с поиском

- **Проблемный источник:**
  - Если `is_problematic === true` - красная рамка или предупреждение
  - Иконка ⚠️ рядом с названием

### Валидация
- URL должен быть валидным (начинаться с http:// или https://)
- Название должно быть уникальным (проверка на бэкенде)
- При ошибке валидации показывать сообщение под полем

### Автосохранение (опционально)
- Можно реализовать автосохранение при изменении полей
- Или показывать индикатор "Есть несохраненные изменения"

## Примеры использования

### Пример 1: Изменение языка источника
1. Пользователь открывает карточку источника
2. Переходит на вкладку "Настройки поиска"
3. Выбирает язык из выпадающего списка (например, "Spanish")
4. Нажимает "Сохранить"
5. Отправляется PATCH запрос: `PATCH /api/references/resources/123/` с телом `{"language": "es"}`
6. Карточка закрывается, таблица обновляется

### Пример 2: Добавление кастомных инструкций
1. Пользователь открывает карточку проблемного источника
2. Переходит на вкладку "Настройки поиска"
3. Меняет тип на "Гибридный"
4. В поле "Кастомные инструкции" вводит специальные инструкции
5. Сохраняет изменения
6. Отправляется PATCH запрос с обновленными полями

### Пример 3: Добавление служебных заметок
1. Пользователь открывает карточку источника
2. Переходит на вкладку "Служебное"
3. В поле "Служебные заметки" записывает: "Проблема: сайт часто недоступен. Решение: пометить как manual"
4. Сохраняет изменения

## Примеры кода

### React компонент (псевдокод)

```typescript
interface ResourceCardProps {
  resourceId: number;
  onClose: () => void;
}

const ResourceCard: React.FC<ResourceCardProps> = ({ resourceId, onClose }) => {
  const [resource, setResource] = useState<NewsResource | null>(null);
  const [loading, setLoading] = useState(true);
  const [saving, setSaving] = useState(false);
  const [activeTab, setActiveTab] = useState('basic');

  useEffect(() => {
    // Загрузка данных источника
    fetch(`/api/references/resources/${resourceId}/`)
      .then(res => res.json())
      .then(data => {
        setResource(data);
        setLoading(false);
      });
  }, [resourceId]);

  const handleSave = async () => {
    setSaving(true);
    try {
      const response = await fetch(`/api/references/resources/${resourceId}/`, {
        method: 'PATCH',
        headers: {
          'Content-Type': 'application/json',
          'Authorization': `Bearer ${getAuthToken()}`
        },
        body: JSON.stringify({
          language: resource.language,
          custom_search_instructions: resource.custom_search_instructions,
          internal_notes: resource.internal_notes,
          source_type: resource.source_type
        })
      });
      
      if (response.ok) {
        showToast('Изменения сохранены');
        onClose();
      } else {
        const errors = await response.json();
        showValidationErrors(errors);
      }
    } catch (error) {
      showError('Ошибка при сохранении');
    } finally {
      setSaving(false);
    }
  };

  if (loading) return <LoadingSpinner />;

  return (
    <Modal onClose={onClose}>
      <Card>
        <Header>
          <Title>{resource.name}</Title>
          <URL href={resource.url} target="_blank">{resource.url}</URL>
        </Header>
        
        <Tabs value={activeTab} onChange={setActiveTab}>
          <Tab value="basic">Основное</Tab>
          <Tab value="settings">Настройки поиска</Tab>
          <Tab value="internal">Служебное</Tab>
          <Tab value="statistics">Статистика</Tab>
        </Tabs>
        
        <TabPanel value="basic">
          {/* Основная информация */}
        </TabPanel>
        
        <TabPanel value="settings">
          <LanguageSelector 
            value={resource.language}
            onChange={(lang) => setResource({...resource, language: lang})}
          />
          <SourceTypeSelector 
            value={resource.source_type}
            onChange={(type) => setResource({...resource, source_type: type})}
          />
          {resource.source_type === 'hybrid' && (
            <CustomInstructionsField
              value={resource.custom_search_instructions}
              onChange={(val) => setResource({...resource, custom_search_instructions: val})}
            />
          )}
        </TabPanel>
        
        <TabPanel value="internal">
          <InternalNotesField
            value={resource.internal_notes}
            onChange={(val) => setResource({...resource, internal_notes: val})}
          />
        </TabPanel>
        
        <TabPanel value="statistics">
          <StatisticsDisplay data={resource.statistics} />
        </TabPanel>
        
        <Footer>
          <Button onClick={onClose}>Отмена</Button>
          <Button primary onClick={handleSave} disabled={saving}>
            {saving ? 'Сохранение...' : 'Сохранить'}
          </Button>
        </Footer>
      </Card>
    </Modal>
  );
};
```

## Технические детали

### Компоненты
- `ResourceCard` - основной компонент карточки
- `ResourceTabs` - компонент вкладок
- `ResourceBasicInfo` - форма основной информации
- `ResourceSearchSettings` - форма настроек поиска
- `ResourceInternalNotes` - поле служебных заметок
- `ResourceStatistics` - отображение статистики

### Состояние
- Использовать локальное состояние для формы
- При открытии карточки загружать данные источника через API
- При сохранении отправлять PATCH запрос

### Обработка ошибок
- Показывать toast/уведомление при успешном сохранении
- Показывать ошибку валидации под соответствующим полем
- Показывать общую ошибку при проблемах с сетью

## Дополнительные возможности (опционально)

1. **История изменений** - показывать когда и что было изменено
2. **Предпросмотр промпта** - показывать как будет выглядеть промпт для этого источника (на основе языка и кастомных инструкций)
3. **Тестовый поиск** - кнопка для тестового запуска поиска новостей (требует отдельный endpoint)
4. **Экспорт данных** - экспорт информации об источнике в JSON/CSV
5. **Быстрые действия:**
   - Кнопка "Пометить как проблемный" (добавляет заметку в internal_notes)
   - Кнопка "Переключить тип" (auto ↔ manual ↔ hybrid)
   - Кнопка "Сбросить статистику" (требует подтверждения)

## Валидация полей

### Название (name)
- Обязательное поле
- Максимальная длина: 255 символов
- Должно быть уникальным (проверка на бэкенде)

### URL
- Обязательное поле
- Должен начинаться с `http://` или `https://`
- Валидация формата URL на бэкенде

### Язык (language)
- Обязательное поле
- Допустимые значения: `ru`, `en`, `es`, `de`, `pt`, `fr`, `it`, `tr`, `ar`, `zh`, `ja`, `ko`, `pl`, `nl`, `sv`, `other`
- По умолчанию: `en`

### Тип источника (source_type)
- Обязательное поле
- Допустимые значения: `auto`, `manual`, `hybrid`
- По умолчанию: `auto`

### Кастомные инструкции (custom_search_instructions)
- Необязательное поле
- Многострочный текст
- Рекомендуется использовать для источников типа `hybrid`

### Служебные заметки (internal_notes)
- Необязательное поле
- Многострочный текст
- Только для администраторов

## Дизайн

- Использовать современный Material Design или аналогичный стиль
- Адаптивный дизайн (работает на мобильных устройствах)
- Темная/светлая тема (если реализована в приложении)
- Плавные анимации открытия/закрытия карточки
