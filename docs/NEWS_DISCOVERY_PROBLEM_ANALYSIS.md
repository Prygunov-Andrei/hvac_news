# Анализ проблемы: LLM не находит новости

## Проблема

После запуска поиска новостей для 137 источников система создала 137 записей "Новостей не найдено", хотя реально на многих источниках должны быть новости за указанный период.

## Причина

**Основная проблема:** Старые модели LLM (OpenAI GPT-4o и Google Gemini) **не могут реально искать новости на веб-сайтах** без доступа к интернету или специальных инструментов для веб-поиска.

## Решение

**Использование ChatGPT-5.2 Pro:**
- Обновлена модель на `gpt-5.2-pro` (вместо `gpt-4o`)
- GPT-5.2 имеет встроенную поддержку веб-поиска
- Промпт обновлен для обязательного использования веб-поиска (без использования знаний LLM)

### Что происходит:

1. Промпт просит LLM найти новости на конкретном URL (например, `https://ashrae.org`)
2. LLM не может загрузить содержимое веб-страницы без доступа к интернету
3. LLM возвращает пустой массив `{"news": []}`, потому что не может выполнить задачу
4. Система создает запись "Новостей не найдено"

### Тестирование показало:

```python
OpenAI ответ: {'news': []}  # Пустой массив для всех источников
```

## Решения

### ✅ Решение 1: Использовать LLM с доступом к интернету (РЕАЛИЗОВАНО)

**OpenAI GPT-4o:**
- Поддерживает веб-поиск через Responses API с инструментом `web_search`
- Требует использования специального формата запроса
- **Статус:** Обновлен код для поддержки (но может требовать дополнительной настройки)

**Google Gemini:**
- Поддерживает "Grounding with Google Search" через параметр `google_search_retrieval`
- **Стоимость:** $35 за 1000 запросов (платная функция)
- **Статус:** Обновлен код для поддержки

### 🔄 Решение 2: Использовать Google Search API / Bing Search API (РЕКОМЕНДУЕТСЯ)

**Подход:**
1. Использовать Google Search API или Bing Search API для поиска новостей
2. Формировать поисковые запросы вида: `site:ashrae.org HVAC news 2025-12-18`
3. Получать результаты поиска (ссылки, заголовки, сниппеты)
4. Отправлять результаты к LLM для анализа и суммаризации

**Преимущества:**
- Реальный поиск по интернету
- Актуальные результаты
- Работает для всех источников

**Недостатки:**
- Требует API ключи для Google/Bing Search
- Дополнительные затраты на API

### 🔄 Решение 3: Веб-скрапинг + LLM анализ

**Подход:**
1. Использовать веб-скрапинг (например, `requests` + `BeautifulSoup` или `Selenium`)
2. Загружать содержимое страниц источников
3. Извлекать новости за указанный период
4. Отправлять контент к LLM для суммаризации

**Преимущества:**
- Прямой доступ к контенту сайтов
- Точные результаты

**Недостатки:**
- Сложно для динамических сайтов (JavaScript)
- Может нарушать правила использования сайтов (robots.txt)
- Требует обработки разных форматов сайтов
- Медленнее для большого количества источников

### ⚠️ Решение 4: Изменить промпт (ВРЕМЕННОЕ РЕШЕНИЕ)

**Подход:**
- Изменить промпт так, чтобы LLM использовал свои знания о новостях HVAC индустрии
- LLM будет пытаться найти релевантные новости на основе своих знаний

**Недостатки:**
- Не гарантирует актуальность
- Может пропускать реальные новости
- Не работает для новых источников

## Текущее состояние

### Что исправлено:

1. ✅ Обновлен промпт для указания использования веб-поиска
2. ✅ Добавлена поддержка веб-поиска для OpenAI (требует проверки)
3. ✅ Добавлена поддержка Google Search grounding для Gemini (требует платной подписки)

### Что нужно сделать:

1. **Проверить работу веб-поиска:**
   - Протестировать OpenAI Responses API с web_search
   - Проверить доступность Google Search grounding для Gemini

2. **Если веб-поиск не работает:**
   - Реализовать решение через Google Search API / Bing Search API
   - Или реализовать веб-скрапинг для основных источников

3. **Альтернативное решение:**
   - Использовать RSS-фиды источников (если доступны)
   - Использовать API источников (если доступны)

## Рекомендации

**Краткосрочное решение:**
- Использовать Google Search API для поиска новостей
- Формировать запросы: `site:{url} HVAC news {date}`

**Долгосрочное решение:**
- Комбинированный подход:
  - RSS-фиды для источников, которые их предоставляют
  - Google Search API для остальных
  - Веб-скрапинг для критически важных источников

## Тестирование

После применения исправлений нужно протестировать:

```bash
# Тест одного источника
python manage.py shell -c "
from references.models import NewsResource
from news.discovery_service import NewsDiscoveryService
from news.models import NewsDiscoveryRun
from django.utils import timezone

resource = NewsResource.objects.filter(url__icontains='ashrae').first()
service = NewsDiscoveryService()
created, errors, msg = service.discover_news_for_resource(resource)
print(f'Создано: {created}, Ошибок: {errors}')
"
```

## Следующие шаги

1. Протестировать обновленный код с веб-поиском
2. Если не работает - реализовать Google Search API интеграцию
3. Добавить логирование для отладки
4. Обновить документацию после решения проблемы
